# SDK playground

Demonstrates shell-brokered storage, deterministic randomness, and managed AI with visible error
handling. Managed AI requires a signed-in player and an enabled allowance. ZIP this folder's
contents and upload them to Prototir.
