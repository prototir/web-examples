# Three.js starter

A responsive rotating scene using the curated `three@0.170.0` platform module. The import map is
created from `prototir.json` during upload. ZIP this folder's contents and upload them to Prototir.
